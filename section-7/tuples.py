# tuples are immutable!!! YAY!!!!!!
someTupple = ('a', 'b', 'c')
someList = [1, 2, 3]

# you can unpack tuples kind of like how you can in es6 but since there is no key it goes by order
# Maybe dict's can be unpacked by key
a, b, c = someTupple

print(a)
print(b)
print(c)
